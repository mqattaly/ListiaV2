# انتقال داده از نسخه‌ی پایتون به نسخه‌ی جدید (React + Node)

نسخه‌ی پایتون شما روی **دیتابیس ابری PostgreSQL چابکان** اجرا می‌شود — این راهنما دقیقاً برای همین حالت نوشته شده. (حالت فایل SQLite هم آخر راهنما آمده است.)

## چه چیزی منتقل می‌شود؟

| داده | وضعیت |
|---|---|
| کاربران | ✅ با **همان نام کاربری و همان رمز عبور قدیمی** (نیازی به تغییر رمز نیست) |
| وضعیت لایسنس، کلید لایسنس، تاریخ انقضا، سهمیه‌ی برآورد | ✅ عیناً |
| تامین‌کنندگان | ✅ |
| کالاها (نام، تعداد، واحد، توضیح، قیمت واحد، …) | ✅ |
| **خریدهای زده‌شده و آرشیو شده + تاریخ هر خرید** | ✅ |
| اشتراک‌گذاری دیتابیس بین کاربران | ✅ |
| ردیف‌های خیلی قدیمیِ بی‌صاحب | ✅ به اولین کاربر منتسب می‌شوند (مثل خود نسخه‌ی پایتون) |

> این انتقال **فقط خواندن** از دیتابیس قدیمی است — به سرویس PostgreSQL قدیمی هیچ چیزی نوشته یا تغییر داده نمی‌شود؛ می‌توانید بعداً آن را به‌عنوان بکاپ نگه دارید یا حذفش کنید.

---

## قدم ۱ — پیدا کردن آدرس اتصال به دیتابیس قدیمی

سرویس پایتونی قدیمی با متغیر `DATABASE_URL` به دیتابیس وصل است. آن را بردارید:

- **پنل چابکان → سرویس پایتونی قدیمی → تنظیمات (متغیرهای محیطی)** → مقدار `DATABASE_URL` را کپی کنید
- شکلش چیزی شبیه این است: `postgresql://caravan_user:PASSWORD@HOST:PORT/DBNAME`

اگر آن‌جا نبود: **داشبورد سرویس دیتابیس PostgreSQL** را باز کنید → اطلاعات اتصال (هاست، پورت، نام کاربری، رمز، نام دیتابیس) آن‌جاست و آدرس را خودتان این‌طور بسازید:

```
postgresql://نام‌کاربری:رمز@هاست:پورت/نام‌دیتابیس
```

## قدم ۲ — اجرای انتقال روی سرویس جدید

1. مطمئن شوید سرویس جدید **یک‌بار بالا آمده** و صفحه‌ی ورود باز می‌شود
2. مطمئن شوید کد سرویس جدید به‌روز است (این قابلیت در نسخه‌ای است که `tools/migrate-from-python.mjs` و وابستگی `pg` را دارد — اگر ریپو را قبل از این تغییر آپلود کرده‌اید، فایل‌های جدید را آپلود و سرویس را Rebuild کنید)
3. پنل چابکان → سرویس جدید → **کنسول** → این دستور را بزنید (کل آدرس داخل گیومه!)

   ```bash
   node tools/migrate-from-python.mjs "postgresql://caravan_user:PASSWORD@HOST:PORT/DBNAME"
   ```

4. باید این گزارش را ببینید:

   ```
   ✅ انتقال کامل شد:
      کاربران:        12  (رمزهای قدیمی هنوز کار می‌کنند)
      تامین‌کنندگان:  30
      کالاها:         150  (خریدهای آرشیوشده و تاریخشان حفظ شد)
      اشتراک‌گذاری:   2
   ```

5. سرویس را **ری‌استارت** کنید
6. وارد شوید — با همان نام‌ها و رمزهای قدیمی ✓

> اجرای دوباره‌ی دستور امن است؛ هیچ داده‌ی تکراری ساخته نمی‌شود و فقط کالاهای جدید اضافه می‌شوند.
> اگر روی سرویس جدید تا الان اکانتی ساخته‌اید که در نسخه‌ی قدیمی هم هست، ادغام/به‌روزرسانی می‌شود.

## از این به بعد دیتابیس کجاست؟

نسخه‌ی جدید به **SQLite روی دیسک مانا** خودش کار می‌کند (فایل `listia.db` روی دیسکی که موقع استقرار وصل کردید) — دیگر به سرویس PostgreSQL نیازی نیست:

- بکاپ روزانه = بکاپ دیسک مانا در چابکان + دانلود گاه‌به‌گاه فایل `listia.db`
- سرویس PostgreSQL قدیمی دست‌نخورده می‌ماند — تا مدتی نگهش دارید (بکاپ زنده‌ی داده‌های قدیمی است) و بعداً اگر خواستید پلنش را حذف کنید

---

## عیب‌یابی

| نشانه | راه‌حل |
|---|---|
| `اتصال به PostgreSQL برقرار نشد` | آدرس را کامل کپی کردید؟ (هایفن و @ و پورت) — دستور را با گیومه دور آدرس بزنید |
| `password authentication failed` | رمز دیتابیس اشتباه است — از داشبورد سرویس دیتابیس چک کنید |
| `relation "user" does not exist` | به دیتابیس اشتباهی وصل شده‌اید — نام دیتابیس انتهای آدرس را چک کنید |
| اتصال timeout | از **کنسول همان سرویس جدید** اجرا کنید (شبکه‌ی داخلی چابکان)، نه از سیستم شخصی — اگر باز هم نشد، هر دو سرویس باید در یک دیتاسنتر/پروژه باشند؛ از پشتیبانی چابکان بپرسید |
| رمز قدیمی کار نمی‌کند | کد سرویس جدید باید اصلاحیه‌ی سازگاری هش Werkzeug را داشته باشد — Rebuild با آخرین نسخه |
| چیزی از آمار کم/زیاد است | رفرش کامل مرورگر (Ctrl+Shift+R) — کش است، نه داده |

---

## پیوست: اگر دیتابیس قدیمی فایل SQLite بود (الان مربوط به شما نیست)

اگر نسخه‌ی پایتون را جایی با فایل `purchase.db` (پوشه‌ی `instance`) اجرا می‌کردید، همان فایل را در ریشه‌ی سرویس جدید آپلود کنید و بزنید:

```bash
node tools/migrate-from-python.mjs purchase.db
```

اسکریپت خودش تشخیص می‌دهد منبع فایل است یا آدرس PostgreSQL.
