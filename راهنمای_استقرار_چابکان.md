# آموزش کامل استقرار لیستیا روی چابکان (سرویس Docker)

این راهنما مخصوص ریپوی خودتان نوشته شده: **[github.com/mqattaly/ListiaV2](https://github.com/mqattaly/ListiaV2)** (عمومی) — شاخه‌ی `main` همیشه آخرین نسخه‌ی اپ است.

---

## آن «ایمیج» که از شما می‌خواهد چیست؟

وقتی سرویس Docker می‌سازید، چابکان دو گزینه جلویتان می‌گذارد:

| گزینه | یعنی چه | مناسب شما؟ |
|---|---|---|
| **«می‌خواهم از داکر image استفاده کنم»** | یک ایمیج **آماده‌ی ساخته‌شده** از رجیستری (مثل Docker Hub) آدرسش را بدهید؛ چابکان فقط دانلودش می‌کند | ❌ لازم نیست — شما Dockerfile دارید |
| **«می‌خواهم Dockerfile شخصی خودم را ایجاد کنم»** | خودتان Dockerfile می‌دهید و چابکان **خودش ایمیج را می‌سازد** | ✅ همین را انتخاب کنید |

پس آن فیلد «آدرس image» را خالی بگذارید و گزینه‌ی **Dockerfile** را انتخاب کنید. (روش ایمیج آماده را هم آخر همین راهنما آورده‌ام، برای وقتی که خواستید.)

---

## مرحله‌ی ۱ — ساخت سرویس (۲ دقیقه)

1. پنل چابکان → **سرویس‌ها → ایجاد سرویس** → نوع **Docker** → پلن خریداری‌شده
2. اسم سرویس: مثلاً `listia`
3. در مرحله‌ی پیکربندی: **«می‌خواهم Dockerfile شخصی خودم را ایجاد کنم»** را انتخاب کنید
4. متن Dockerfile — این را کپی کنید (همان فایل `Dockerfile` ریشه‌ی پروژه است):

```dockerfile
# ─── لیستیا — ایمیج پروداکشن برای چابکان ────────────────────────────────────
# مرحله ۱: ساخت فرانت‌اند (React/Vite)
FROM node:22-bookworm-slim AS web
WORKDIR /app
COPY package.json package-lock.json ./
COPY server/package.json server/package.json
COPY client/package.json client/package.json
RUN npm ci --no-audit --no-fund
COPY client client
RUN npm run build -w client

# مرحله ۲: فقط وابستگی‌های اجرا (بدون ابزارهای توسعه)
FROM node:22-bookworm-slim AS deps
WORKDIR /app
COPY package.json package-lock.json ./
COPY server/package.json server/package.json
COPY client/package.json client/package.json
RUN npm ci --omit=dev --no-audit --no-fund

# مرحله ۳: ایمیج نهایی
FROM node:22-bookworm-slim
WORKDIR /app
ENV NODE_ENV=production \
    PORT=3000
COPY --from=deps /app/node_modules ./node_modules
COPY server ./server
COPY --from=web /app/client/dist ./client/dist
EXPOSE 3000
HEALTHCHECK --interval=30s --timeout=5s --retries=3 \
  CMD node -e "fetch('http://127.0.0.1:3000/healthz').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
CMD ["sh", "-c", "mkdir -p \"$(dirname \"$LISTIA_DB\")\" 2>/dev/null; exec node --no-warnings server/index.js"]
```

5. سرویس را بسازید — فعلاً بالانمی‌آید (فایل‌های سورس هنوز نیست)؛ اشکالی ندارد، مرحله‌ی بعد را انجام دهید.

---

## مرحله‌ی ۲ — تنظیم پورت (همین اول، که یادتان نرود)

1. وارد داشبورد سرویس شوید → منوی راست → **پورت‌ها**
2. **پورت پیش‌فرض** سرویس را از 80 روی **3000** بگذارید (اپ داخل کانتینر روی 3000 گوش می‌دهد و دامنه‌ی پیش‌فرض سرویس به همین پورت اشاره می‌کند)

---

## مرحله‌ی ۳ — ساخت دیسک مانا (مهم‌ترین مرحله — دیتابیس!)

بدون این مرحله، **با هر ری‌استارت یا دپلوی، همه‌ی کاربران و داده‌ها پاک می‌شوند!**

1. داشبورد سرویس → منوی راست → **دیسک‌ها** → «افزودن دیسک جدید»
2. مسیر **مقصد** (داخل کانتینر) را بگذارید: `/app/server/data`
   — دیتابیس SQLite دقیقاً همان‌جا ساخته می‌شود (`listia.db`)
3. سایز دیسک: حداقل ۱ گیگابایت (پلن شما هرچند اجازه می‌دهد)

> چرا این مسیر؟ چون اگر متغیر `LISTIA_DB` را ست نکنید، سرور به‌صورت پیش‌فرض دیتابیس را در `server/data` داخل اپ می‌سازد. اگر ترجیح می‌دهید مسیر جدا باشد، می‌توانید مقصد دیسک را `/data` بگذارید و متغیر محیطی `LISTIA_DB=/data/listia.db` را در مرحله‌ی ۴ ست کنید — هر دو درست کار می‌کند.

---

## مرحله‌ی ۴ — متغیرهای محیطی

داشبورد سرویس → **تنظیمات** (یا «متغیرهای محیطی») → این‌ها را اضافه کنید:

| متغیر | مقدار | ضرورت |
|---|---|---|
| `SECRET_KEY` | یک رشته‌ی تصادفی طولانی (مثلاً از `randomkeygen` یا ۳۲+ کاراکتر دستی) | ⚠️ ضروری — بدون آن، با هر ری‌استارت همه از حساب بیرون می‌افتند |
| `ADMIN_USERNAME` | مثلاً `smq2458` | پیشنهادی — مدیر اولیه با این نام ساخته می‌شود |
| `ADMIN_PASSWORD` | یک رمز قوی | **در production ضروری است** — بدون آن ساخت خودکار ادمین انجام نمی‌شود (در لاگ هشدار می‌بینید) |
| `NODE_ENV` | `production` | پیشنهادی |
| `SMTP_HOST` | مثال `mail.chabookan.ir` | برای ثبت‌نام کاربران جدید (ارسال کد تأیید) |
| `SMTP_PORT` | معمولاً `587` (یا `465`) | 〃 |
| `SMTP_USERNAME` | نشانی کامل ایمیل، مثل `info@listia.ir` | 〃 |
| `SMTP_PASSWORD` | رمز همان صندوق ایمیل | 〃 |
| `SMTP_FROM` | همان نشانی ایمیل فرستنده | اختیاری؛ پیش‌فرض = SMTP_USERNAME |
| `SMTP_USE_SSL` | برای ۴۶۵ مقدار `1`؛ برای ۵۸۷ مقدار `0` (STARTTLS خودکار) | 〃 |

> **پورت‌ها:** پورت **۵۸۷ با STARTTLS** رایج‌ترین حالت سرویس‌های ایمیل ایرانی (مثل چابوکان) است؛
> پورت **۴۶۵ با SSL** حالت دیگر است. مقدار `SMTP_USE_SSL` را مطابق پورت بگذارید.
>
> **تست ایمیل بعد از استقرار:** وارد ترمینال/کنسول سرویس شوید و اجرا کنید:
> `node server/mail-test.mjs` — هم اتصال و احراز هویت را تست می‌کند هم یک ایمیل آزمایشی می‌فرستد.
> در لاگ بوت هم باید خط `📧 SMTP پیکربندی شده: …` دیده شود.
>
> **دسترسی شبکه:** سرویس چابکان باید اجازه‌ی اتصال خروجی به اینترنت (پورت ۵۸۷ یا ۴۶۵) را داشته باشد؛
> اگر ایمیل نمی‌رود و در لاگ «زمان ارسال ایمیل تمام شد» یا «Connection closed» می‌بینید،
> از تنظیمات شبکه/فایروال سرویس در داشبورد چابکان، دسترسی خروجی به سرور ایمیل را فعال کنید.
>
> **اگر فعلاً SMTP ندارید:** ثبت‌نامِ کاربر جدید کد تأیید نمی‌فرستد (ولی ورود با اکانت مدیر و
> ساخت کاربر/لایسنس از پنل مدیریت کار می‌کند). بعداً همین متغیرها را پر و سرویس را ری‌استارت کنید.

### جلوگیری از رفتن ایمیل به پوشه‌ی هرزنامه (Spam) — حتماً تنظیم کنید

کد برنامه هم نسخه‌ی متنی و هم HTML استاندارد می‌فرستد، نشانی فرستنده همان حساب احرازشده است و
هدرهای معتبر دارد؛ اما تصمیم «صندوق ورودی در برابر هرزنامه» در جی‌میل/اوت‌لوک عمدتاً به **رکوردهای
DNS دامنه‌ی فرستنده** بستگی دارد. روی دامنه‌ی `listia.ir` (از پنل DNS یا همان پنل ایمیل چابوکان):

1. **SPF** — یک رکورد TXT که سرور ایمیل را مجاز به ارسال از دامنه می‌کند. مقدار دقیق را پنل ایمیل
   چابوکان می‌دهد؛ معمولاً شبیه این است (اگر سرویس دیگری هم ایمیل می‌فرستد رکوردهای قبلی را حفظ کنید):
   `v=spf1 include:_spf.chabookan.ir ~all` (به جای `_spf.chabookan.ir` مقداری که چابوکان در بخش
   «رکورد SPF» نشان می‌دهد).
2. **DKIM** — در پنل ایمیل چابوکان (DirectAdmin → E-Mail Accounts / DKIM یا «امضای ایمیل») امضای
   DKIM را فعال کنید و رکورد TXT عمومی که می‌دهد را در DNS دامنه اضافه کنید. مهم‌ترین عامل جیمیل است.
3. **DMARC** — یک رکورد TXT روی `_dmarc.listia.ir`:
   `v=DMARC1; p=none; rua=mailto:info@listia.ir` (ابتدا `p=none`؛ بعد از اطمینان می‌توانید `quarantine` کنید).
4. نشانی فرستنده (`SMTP_FROM` و `SMTP_USERNAME`) حتماً روی همان دامنه باشد (مثل `info@listia.ir`).
5. **rDNS/PTR:** آی‌پی سرور باید به نام دامنه برگردد؛ روی سرویس اشتراکی چابوکان معمولاً از قبل تنظیم است —
   اگر بعد از سه مورد بالا باز هم اسپم شد، از پشتیبانی چابوکان بپرسید.

تست سریع بعد از تنظیم: یک ثبت‌نام با ایمیل **جی‌میل** انجام دهید، ایمیل را باز کنید و از منوی
«نمایش پیام اصلی / Show original» وضعیت `SPF: pass` و `DKIM: pass` و `DMARC: pass` را ببینید.
هر کدام `fail`/`none` بود، همان رکورد DNS هنوز کامل/پخش نشده است (پخش DNS ممکن است چند ساعت طول بکشد).

> ایمیل‌های تراکنشی (کد تأیید) هدر تبلیغاتی ندارند؛ ایمیل خوش‌آمد طبق توصیه‌ی جیمیل هدر
> `List-Unsubscribe` دارد و نسخه‌ی متنی همیشه همراه HTML فرستاده می‌شود.

---

## مرحله‌ی ۵ — آپلود کد از گیت

ریپوی **ListiaV2 عمومی** است؛ کلون مستقیم و بدون هیچ توکنی کار می‌کند.

### آپلود در کنسول چابکان

1. داشبورد سرویس → منوی راست → **کنسول** (یک ترمینال وب باز می‌شود)
2. این دستورها را به‌ترتیب بزنید:

```bash
# پاک‌کردن فایل‌های پیش‌فرض چابکان (فقط بار اول)
rm -rf $(ls -A)

# کلون ریپو (بدون توکن — چون عمومی است)
git clone https://github.com/mqattaly/ListiaV2.git tmp

# آوردن فایل‌ها به ریشه‌ی سرویس
cd tmp && mv $(ls -A) ../ && cd ../ && rm -rf tmp
```

> حجم کل پروژه ~۲ مگابایت است؛ سقف ۱۰۰ مگابایت روش گیت مشکلی ندارد.
> بعد از انتقال فایل‌ها `rm -rf .git` بزنید تا تاریخچه‌ی گیت داخل سرویس ذخیره نشود.

### اگر روزی ریپو را خصوصی کردید (الان لازم نیست)

کلون بدون توکن آن‌وقت خطای `Authentication failed` می‌دهد و باید یک توکن فقط-خواندنی بسازید: GitHub → Settings → Developer settings → Personal access tokens (Fine-grained) — دسترسی: فقط ریپوی ListiaV2 با **Contents → Read-only** — و آدرس کلون را این‌طور بزنید (بدون فاصله بین توکن و `@`):
`git clone https://github_pat_XXXX@github.com/mqattaly/ListiaV2.git tmp`

---

## مرحله‌ی ۶ — بیلد و بالا آمدن

1. داشبورد سرویس → **ری‌استارت** بزنید (یا از منو «Rebuild»)
2. بروید به **لاگ‌ها** — باید این‌ها را ببینید:
   - دانلود ایمیج پایه‌ی `node:22`
   - `npm ci` و `vite build` (بیلد فرانت‌اند)
   - در آخر: `🚀 سرور لیستیا روی پورت 3000 بالا آمد`
3. اولین اجرا این پیام هم می‌آید: `👑 حساب مدیر اولیه ساخته شد → admin / admin123456`

مقدار زمان بیلد اول: معمولاً ۲ تا ۵ دقیقه.

---

## مرحله‌ی ۷ — ورود و تست

1. آدرس سرویس را از داشبورد بردارید (چیزی مثل `https://listia-xxx.services.irn1.chabokan.net` — چابکان خودش HTTPS می‌دهد)
2. `https://آدرس-سرویس/healthz` باید `{"status":"ok"}` بدهد — تست سریع سلامت
3. صفحه‌ی اصلی را باز کنید → با مدیر وارد شوید → **همان لحظه رمز را از «حساب کاربری → تغییر رمز» عوض کنید**
4. از منوی کاربر (بالای صفحه) → **پنل مدیریت** → برای هر مشتری: ویرایش کاربر / اعطای لایسنس / تولید کلید
5. iOS: در Safari همان آدرس را باز کنید → Share → **افزودن به صفحه اصلی** — وب‌اپ نصب می‌شود

---

## بکاپ و بازگردانی دیتابیس

کل داده‌ی شما **یک فایل** است: `listia.db` داخل دیسک مانا (`/app/server/data`).

- **گرفتن بکاپ**: داشبورد سرویس → **مدیریت فایل‌ها** → پوشه‌ی `server/data` (یا مسیر دیسک) → فایل `listia.db` را دانلود کنید
- (اگر `listia.db-wal` و `listia.db-shm` هم بودند، آن‌ها را هم بردارید — یا قبل از دانلود، سرویس را یک دقیقه ری‌استارت کنید تا WAL داخل فایل اصلی برود)
- **بازگردانی**: فایل بکاپ را در همان مسیر جایگزین کنید → ری‌استارت
- **بکاپ خودکار**: چابکان برای دیسک‌ها قابلیت بکاپ خودکار دارد (داشبورد → دیسک‌ها → تنظیمات بکاپ) — روشن‌کردنش کافی است

## به‌روزرسانی اپ (بعد از تغییر کد)

```bash
# در کنسول سرویس:
git pull        # (اگر .git را نگه داشته‌اید)
# و اگر .git را پاک کرده‌اید، دوباره کلون و انتقال مثل مرحله‌ی ۵
```
سپس داشبورد → **ری‌استارت/Rebuild**. دیتابیس روی دیسک است و دست‌نخورده می‌ماند.

---

## عیب‌یابی

| نشانه | علت و راه‌حل |
|---|---|
| 502 / بالا نمی‌آید | لاگ‌ها را ببینید؛ اگر `npm ci` خطای lockfile داد، یعنی کد کامل کلون نشده — مرحله‌ی ۵ را از اول |
| `unable to open database file` | دیسک ساخته نشده یا مسیرش اشتباه است — مرحله‌ی ۳ (نسخه‌های جدید سرور خودش پوشه را می‌سازند) |
| صفحه سفید / 404 | فرانت‌اند داخل ایمیج ساخته نشده — لاگ بیلد را چک کنید؛ باید `vite build` دیده شود |
| بعد از ری‌استارت همه بیرون افتادند | `SECRET_KEY` ست نشده — مرحله‌ی ۴ |
| بعد از ری‌استارت داده‌ها پاک شد | دیسک مانا وصل نشده — مرحله‌ی ۳ |
| دانلود ایمیج node:22 در بیلد خطا داد | تحریم/قطعی Docker Hub — از پشتیبانی چابکان بخواهید میرور رجیستری‌شان را فعال کنند (چابکان میرور داخلی دارد) |
| کد تأیید برای کاربر جدید نمی‌آید | اول لاگ بوت: اگر `⚠️ SMTP پیکربندی نشده` است متغیرها را ست نکرده‌اید — مرحله‌ی ۴. اگر `📧 SMTP پیکربندی شده` هست ولی ایمیل نمی‌آید: `node server/mail-test.mjs` را در کنسول سرویس اجرا کنید؛ اگر «Connection closed/زمان ارسال تمام شد» داد یعنی پورت خروجی SMTP بسته است (با پشتیبانی هاست برای باز کردن ۵۸۷/۴۶۵ هماهنگ کنید) یا رمز ایمیل اشتباه است؛ اگر موفق بود پوشه‌ی Spam را چک کنید |
| پیام «حساب ساخته شد اما ارسال ایمیل تایید ناموفق بود» | همان مورد بالا؛ حساب ساخته شده و بعد از درست‌شدن SMTP با دکمه‌ی «ارسال مجدد» کد می‌گیرد |
| خطای `LICENSE_SECRET` موقع بالا آمدن | در production این متغیر (و `SECRET_KEY`) باید ست شده باشد — مرحله‌ی ۴ |

---

## پیوست: روش «ایمیج آماده» (اگر روزی خواستید به‌جای Dockerfile از رجیستری بدهید)

روی سیستم خودتان که Docker دارد:

```bash
cd Listia
docker build -t USERNAME/listia:latest .
docker login            # اکانت Docker Hub
docker push USERNAME/listia:latest
```

سپس موقع ساخت سرویس در چابکان، گزینه‌ی «می‌خواهم از داکر image استفاده کنم» و آدرس: `USERNAME/listia:latest`. ریپوی Docker Hub باید public باشد (یا از پشتیبانی برای ایمیج خصوصی/رجیستری داخلی کمک بگیرید). برای به‌روزرسانی: بیلد و push دوباره + تغییر تگ، بعد در چابکان آدرس ایمیج را ویرایش و ری‌استارت.

**توصیه:** برای شما Dockerfile روش بهتری است — هر آپدیت فقط `git pull` + ری‌استارت، بدون نیاز به سیستم داکر محلی.

---

## پشتیبانی هوش مصنوعی (چت‌بات چابکان)

اپ یک دستیار پشتیبانی فارسی به نام «لیا» دارد که با درگاه هوش مصنوعی چابکان کار می‌کند (سازگار با OpenAI).

۱. در پنل چابکان یک **سرویس هوش مصنوعی** بسازید و اعتبارش را شارژ کنید.
۲. در زبانه‌ی «کلیدهای API» یک کلید بسازید (فرمت `sk-chbk-...`) و در زبانه‌ی «مدل‌های قابل استفاده» شناسه‌ی مدل را یادداشت کنید. پیش‌فرض برنامه مدل رایگان رسمی چابکان یعنی `chabok/free` است و مدل `openai/gpt-oss-20b-free` (تعرفه‌ی تقریباً رایگان ۰٫۱۰/۰٫۴۰ دلار برای هر میلیون توکن و فارسی خوب) به‌صورت خودکار به‌عنوان مدل پشتیبان تنظیم شده؛ در صورت نیاز می‌توانید مدل‌های دیگری را در پنل به سرویس خود اضافه و با متغیرهای محیطی انتخاب کنید.
۳. در متغیرهای محیطی سرویس لیستیا (مرحله‌ی ۴ همین راهنما) این‌ها را اضافه و ری‌استارت کنید:

```
CHABOKAN_AI_API_KEY=sk-chbk-کلید-واقعی-شما
CHABOKAN_AI_MODEL=chabok/free
CHABOKAN_AI_MODEL_FALLBACK=openai/gpt-oss-20b-free
CHABOKAN_AI_BASE_URL=https://ai.chabokan.net/v1
```

نکته‌ها:
- کلید فقط سمت سرور مصرف می‌شود و هیچ‌گاه به مرورگر کاربر نمی‌رود.
- اگر مدل اصلی موقتاً خطا بدهد، درخواست خودکار روی مدل پشتیبان تکرار می‌شود؛ خطای ۴۰۱/۴۰۳ (کلید نامعتبر) مدل‌به‌مدل تکرار نمی‌شود.
- اگر کلید ست نشود، اپ به‌جای خطا، دکمه‌ی پشتیبانی را روی حالت «پشتیبانی انسانی» (دایرکت اینستاگرام و ایمیل) نشان می‌دهد.
- سقف مصرف: حداکثر ۲۰ پیام در دقیقه برای هر کاربر (۴۰ برای هر IP)؛ می‌توانید روی کلید خود در پنل چابکان هم سقف ماهانه بگذارید.
- ربات فقط بر اساس یک دانش‌نامه‌ی ثابت درباره‌ی امکانات، پلن‌ها و راهنمای لیستیا پاسخ می‌دهد و موضوعات نامرتبط را به پشتیبانی انسانی (@listia.ir یا info@listia.ir) هدایت می‌کند؛ متن دانش‌نامه در `server/lib/aiSupport.js` قابل به‌روزرسانی است.
- تست سرور (در کنسول کانتینر، با کلید واقعی):
  ```bash
  node -e "fetch('https://ai.chabokan.net/v1/chat/completions',{method:'POST',headers:{Authorization:'Bearer '+process.env.CHABOKAN_AI_API_KEY,'Content-Type':'application/json'},body:JSON.stringify({model:process.env.CHABOKAN_AI_MODEL,messages:[{role:'user',content:'سلام'}]})}).then(r=>r.json()).then(j=>console.log(j.choices?.[0]?.message?.content||j)).catch(console.error)"
  ```
