# راهنمای کامل: نصب لیستیا روی ویندوز، اندروید و iOS (چابکان)

این راهنما از صفر تا صد: راه‌اندازی سرور روی **چابکان**، دیتابیس و بکاپ، اپ نصبی **ویندوز**، اپ **اندروید** و وب‌اپ **iOS** را قدم‌به‌قدم توضیح می‌دهد.

---

## ۰) نقشه‌ی معماری — اول این را بخوانید

```
                    ┌─────────────────────────────┐
                    │   سرور لیستیا روی چابکان     │  ← Node.js + Express + SQLite
                    │   https://app.example.ir     │     (دیتابیس روی دیسک مانا)
                    └──────────┬──────────────────┘
               ┌───────────────┼───────────────────┐
               ▼               ▼                   ▼
        اپ اندروید (APK)   وب‌اپ iOS (Safari)   اپ ویندوز (EXE)
        Capacitor          «افزودن به صفحه اصلی»  Electron
```

**دو الگوی استفاده:**

| الگو | مناسب برای | دیتابیس |
|---|---|---|
| **ریموت** (پیشنهادی) | همه‌جا به یک دیتا دسترسی دارید؛ چند همکار | روی چابکان (یکی برای همه) |
| **محلی** (فقط ویندوز) | تک‌کاربره، بدون اینترنت | داخل خود ویندوز (`%APPDATA%`) |

اپ ویندوز به‌صورت پیش‌فرض **محلی** است؛ اگر `LISTIA_REMOTE_URL` را ست کنید **ریموت** می‌شود (بخش ویندوز). اپ اندروید و وب‌اپ iOS همیشه ریموت‌اند.

---

## ۱) راه‌اندازی سرور روی چابکان

### ۱-۱) پیش‌نیازها

- اکانت چابکان (chabokan.ir) با یک سرویس جدید
- **دیسک مانا (Persistent Disk)**: دیتابیس SQLite داخل فایل است؛ بدون دیسک مانا، با هر ری‌استارت/دپلوی **همه‌ی داده‌ها پاک می‌شود!** در پنل چابکان به سرویس یک دیسک اضافه کنید و مسیر آن را یادداشت کنید (مثلاً `/var/lib/listia`).
- یک دامنه یا ساب‌دامنه (چابکان خودش HTTPS رایگان می‌دهد)

### ۱-۲) روش داکر (پیشنهادی — ساده‌ترین و مطمئن‌ترین)

فایل `Dockerfile` در ریشه‌ی ریپو آماده است (فرانت‌اند را خودش می‌سازد).

1. کد را push کنید و در پنل چابکان سرویس را از روی ریپو (Dockerfile) بسازید.
2. متغیرهای محیطی (بخش ۱-۴) را در پنل ست کنید.
3. پورت را `3000` بگذارید (یا `PORT` را مطابق پورت سرویس تغییر دهید).
4. Health Check را روی مسیر `/healthz` تنظیم کنید.

### ۱-۳) روش Node.js (اگر داکر نمی‌خواهید)

1. در پنل چابکان سرویس از نوع **Node.js (نسخه ۲۲)** بسازید.
2. چون چابکان فقط `npm install --production` می‌زند، فرانت‌اند باید از قبل ساخته شده باشد:
   ```bash
   # روی سیستم خودتان، در ریشه‌ی پروژه:
   npm install
   npm run build          # خروجی در client/dist
   ```
   سپس خط `client/dist/` را از `.gitignore` حذف کنید، `client/dist` را commit و push کنید. (بعد از دپلوی می‌توانید دوباره برگردانید.)
3. دستور اجرای سرویس (Start Command) در پنل:
   ```
   node --no-warnings server/index.js
   ```
4. پورت سرویس را `3001` بگذارید یا متغیر `PORT` را مطابق آن ست کنید.

### ۱-۴) متغیرهای محیطی (در پنل چابکان)

| متغیر | مقدار نمونه | توضیح |
|---|---|---|
| `PORT` | `3000` | پورتی که سرور گوش می‌دهد |
| `SECRET_KEY` | یک رشته‌ی تصادفی طولانی | امضای نشست‌ها؛ **حتماً ست کنید** وگرنه بعد از هر ری‌استارت همه بیرون می‌افتند |
| `LISTIA_DB` | `/var/lib/listia/listia.db` | مسیر دیتابیس روی **دیسک مانا** — مهم‌ترین تنظیم! |
| `ADMIN_USERNAME` | `smq2458` | نام کاربری مدیر اولیه (فقط بار اول ساخته می‌شود) |
| `ADMIN_PASSWORD` | رمز قوی | رمز مدیر اولیه — بعد از اولین ورود عوضش کنید |
| `NODE_ENV` | `production` | خاموش‌کردن نمایش کد تأیید در پاسخ API |
| `SMTP_HOST` | `mail.example.ir` | برای ارسال کد تأیید ایمیل (در پروداکشن لازم) |
| `SMTP_PORT` | `465` | |
| `SMTP_USERNAME` | `no-reply@example.ir` | |
| `SMTP_PASSWORD` | `•••` | |
| `SMTP_USE_SSL` | `true` | برای پورت 465 |
| `LICENSE_SECRET` | (پیش‌فرض داخل کد) | فقط اگر عوض کرده‌اید باید روی همه‌جا یکسان باشد |

> ⚠️ **بدون SMTP**: ثبت‌نام با ایمیلِ جدید کار نمی‌کند (کد جایی ارسال نمی‌شود). تا زمانی که SMTP ندارید، از اکانت‌های موجود (`admin`/`demo`) استفاده کنید یا اکانت‌ها را از پنل مدیریت بسازید.

### ۱-۵) اولین ورود و لایسنس

- سرور در اولین اجرا اگر هیچ کاربری نباشد، مدیر را با `ADMIN_USERNAME`/`ADMIN_PASSWORD` می‌سازد (پیش‌فرض `admin` / `admin123456`).
- مدیر خودکار لایسنس نامحدود دارد و از منوی کاربر به **پنل مدیریت** می‌رود و برای هر کاربر دیگر (با نام کاربری‌شان) از دکمه‌ی «تولید کلید لایسنس» کلید می‌سازد؛ کاربر کلید را در صفحه‌ی «حساب کاربری» وارد می‌کند.
- بدون لایسنس، هر کاربر سقف آزمایشی دارد: ۱ تأمین‌کننده و ۵ محصول.

### ۱-۶) بکاپ دیتابیس

- دیتابیس یک فایل است: همان مسیر `LISTIA_DB` روی دیسک مانا (مثلاً `/var/lib/listia/listia.db`).
- **بکاپ دستی**: از پنل فایل‌منیجر چابکان فایل `listia.db` (و فایل‌های `listia.db-wal` / `listia.db-shm` اگر بودند) را دانلود کنید.
- **بکاپ خودکار (پیشنهادی)**: یک cronjob روی سرویس که هر شب فایل را با تاریخ کپی کند:
  ```bash
  cp /var/lib/listia/listia.db /var/lib/listia/backup-$(date +%F).db
  ```
  (در چابکان می‌توانید از بخش «کرون‌جاب» سرویس این دستور را با زمان‌بندی روزانه ثبت کنید.)
- **بازگردانی**: فایل بکاپ را جایگزین `listia.db` کنید و سرویس را ری‌استارت کنید.
- انتقال از ویندوز به سرور (برعکس هم همین‌طور): فقط همین فایل را کپی کنید.

### ۱-۷) عیب‌یابی چابکان

| مشکل | راه‌حل |
|---|---|
| 502 بعد از دپلوی | لاگ سرویس را ببینید؛ معمولاً `LISTIA_DB` روی مسیر بدون دسترسی نوشته است — دیسک مانا را چک کنید |
| صفحه سفید | یعنی `client/dist` ساخته/آپلود نشده (روش Node) — بخش ۱-۳ |
| بعد از ری‌استارت همه بیرون افتادند | `SECRET_KEY` ست نشده |
| کد تأیید نمی‌آید | SMTP را تنظیم کنید یا `NODE_ENV=production` را بردارید تا کد در پاسخ بیاید (فقط برای تست!) |
| کندی ورود | عادی است — هش رمز عمداً سنگین است |

---

## ۲) iOS — وب‌اپ (PWA) بدون اپ‌استور

روی آیفون/آیپد لازم نیست (و نمی‌شود بدون مک و اکانت توسعه‌دهنده) اپ استوری ساخت؛ لیستیا **وب‌اپ کاملاً نصب‌شونده** است:

1. در **Safari** (نه Chrome) آدرس اپ را باز کنید: `https://app.example.ir`
2. وارد شوید (`demo / demo123456` یا حساب خودتان)
3. دکمه‌ی **اشتراک‌گذاری (Share □↑)** → **«Add to Home Screen» (افزودن به صفحه اصلی)**
4. نام «لیستیا» را تأیید کنید

نتیجه: آیکون لیستیا روی صفحه‌ی گوشی، اجرای تمام‌صفحه‌ی مستقل مثل یک اپ واقعی، بدون نوار مرورگر. آفلاین هم پوسته‌ی اپ باز می‌شود (داده‌ها وقتی اینترنت وصل شود تازه می‌شوند). به‌روزرسانی اپ هم خودکار است — نیاز به نصب دوباره نیست.

> نکته‌ی اندروید: همین وب‌اپ در کروم اندروید هم با «Add to Home screen» نصب می‌شود؛ اما اگر APK واقعی می‌خواهید بخش بعدی.

---

## ۳) ویندوز — اپ نصبی EXE (Electron)

### ۳-۱) پیش‌نیازها (روی سیستم خودتان — ترجیحاً ویندوز)

- **Node.js نسخه ۲۲ یا بالاتر** (nodejs.org)
- اینترنت برای اولین نصب وابستگی‌ها
- (اختیاری) برای امضای برنامه: گواهی Code Signing — بدون آن هم نصب می‌شود فقط یک هشدار SmartScreen می‌دهد

### ۳-۲) ساخت

```bat
:: ۱) در ریشه‌ی پروژه
npm install
npm run build                 :: ساخت فرانت‌اند → client/dist

:: ۲) پوشه‌ی دسکتاپ
cd desktop
npm install                   :: نصب Electron و ابزار بیلد
npm run dist:win              :: ساخت نصبی NSIS
```

خروجی: **`desktop\release\Listia-Setup-2.1.0.exe`** — همین فایل را به هر سیستمی ببرید و نصب کنید (انتخاب مسیر نصب، میان‌بر دسکتاپ و استارت‌منو دارد).

### ۳-۳) بعد از نصب

- اپ کامل **آفلاین** کار می‌کند: سرور و دیتابیس SQLite داخل خودش است.
- اولین بار یک حساب مدیر با نام `admin` و رمز `admin123456` ساخته می‌شود — **فوراً از صفحه‌ی حساب کاربری عوضش کنید.**
- داده‌ها اینجا ذخیره می‌شوند: `%APPDATA%\لیستیا\data\listia.db`
  (در File Explorer بنویسید `%APPDATA%` و پوشه‌ی «لیستیا» را ببینید. بکاپ = کپی همین فایل.)
- هشدار SmartScreen: چون برنامه امضای دیجیتال ندارد، ویندوز اول می‌پرسد → روی **More info → Run anyway** بزنید.

### ۳-۴) حالت ریموت (اتصال به سرور چابکان)

اگر می‌خواهید اپ ویندوز به دیتای روی چابکان وصل شود (نه دیتابیس محلی)، قبل از ساخت در `desktop` این متغیر را ست کنید:

```bat
set LISTIA_REMOTE_URL=https://app.example.ir
npm run dist:win
```

یا بعد از نصب، یک فایل `listia.url` کنار فایل exe نصب‌شده... (ساده‌ترین راه همان ست‌کردن قبل از بیلد است.)

### ۳-۵) نکته‌ها

- اگر موقع اجرا خطای «node:sqlite» دیدید: نسخه‌ی Electron را در `desktop/package.json` به تازه‌ترین نسخه بگذارید (Electron ≥ 37 دارای Node داخلی 22 است) و دوباره `npm install && npm run dist:win` بزنید.
- بیلد روی لینوکس/مک هم ممکن است (`electron-builder` خودش ویندوزی‌ها را می‌سازد) ولی روی ویندوز بی‌دردسرترین است.
- آیکون برنامه: `desktop\build\icon.ico` — برای تغییر آیکون فقط همین فایل را عوض کنید.

---

## ۴) اندروید — اپ نصبی APK (Capacitor)

اپ اندروید یک پوسته‌ی بومی (WebView) است که به سرور چابکان وصل می‌شود؛ پس **اول بخش ۱ را کامل کنید**.

### ۴-۱) پیش‌نیازها

- **Android Studio** (با SDK و JDK خودش) — developer.android.com/studio
- Node.js 22
- یک گوشی اندرویدی (یا شبیه‌ساز)

### ۴-۲) مراحل

```bash
# ۱) آدرس سرور را تنظیم کنید:
#    android-app/capacitor.config.json → server.url = "https://app.example.ir"

# ۲) در پوشه‌ی android-app:
cd android-app
npm install
npx cap add android        # فقط بار اول — پروژه‌ی اندروید ساخته می‌شود

# ۳) باز کردن در Android Studio:
npx cap open android
```

در Android Studio:

- **تست سریع**: منوی `Build → Build APK(s)` → خروجی در
  `android-app/android/app/build/outputs/apk/debug/app-debug.apk` — مستقیم روی گوشی نصب می‌شود (بخش «نصب از منابع ناشناس» را روشن کنید).
- **نسخه‌ی نهایی امضاشده** (برای توزیع):

```bash
# ساخت کلید امضا (فقط یک بار؛ فایل keystore را گم نکنید!):
keytool -genkey -v -keystore listia.keystore -alias listia -keyalg RSA -keysize 2048 -validity 10000
```

سپس در `android-app/android/app/build.gradle` اضافه کنید:

```gradle
android {
    signingConfigs {
        release {
            storeFile file("../../listia.keystore")   # مسیر فایل کلید
            storePassword "رمز-keystore"
            keyAlias "listia"
            keyPassword "رمز-کلید"
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
        }
    }
}
```

و در نهایت: `Build → Generate Signed Bundle / APK → APK` یا از ترمینال:

```bash
cd android-app/android
./gradlew assembleRelease
# خروجی: android-app/android/app/build/outputs/apk/release/app-release.apk
```

### ۴-۳) نکته‌ها

- آیکون و نام: برای آیکون اختصاصی، عکس 1024×1024 را در `android-app/resources/icon.png` بگذارید و `npx @capacitor/assets generate --android` اجرا کنید.
- هر بار که آدرس سرور عوض شد: `capacitor.config.json` را ویرایش و `npx cap sync android` بزنید.
- ورود کاربر در اپ اندروید ماندگار است (localStorage خود WebView) و مکانیزم نشست چندلایه‌ی اپ (?_lt) هم همانجا کار می‌کند.

---

## ۵) دیتابیس — همه‌چیز در یک فایل

لیستیا از **SQLite** استفاده می‌کند (بدون سرور دیتابیس جدا، بدون هزینه‌ی اضافه). یک فایل:

| محیط | مسیر فایل دیتابیس |
|---|---|
| چابکان | `LISTIA_DB` → روی دیسک مانا، مثلاً `/var/lib/listia/listia.db` |
| ویندوز (محلی) | `%APPDATA%\لیستیا\data\listia.db` |
| توسعه (این ریپو) | `server/data/listia.db` |

- **بکاپ = کپی فایل.** بازگردانی = جایگزینی.
- برای آوردن دیتای نمونه در محیط جدید: `npm run seed` (در ریشه، برای توسعه) — حساب‌های `admin/admin123456` و `demo/demo123456` می‌سازد.
- همگام‌سازی بین ویندوزِ محلی و سرور ممکن نیست (دو دیتابیس جدا هستند). اگر یک دیتابیس مشترک می‌خواهید، همه را ریموت کنید (بخش ۳-۴).

---

## ۶) چک‌لیست نهایی دیپلوی

- [ ] سرویس روی چابکان بالا آمده و `/healthz` جواب می‌دهد
- [ ] `LISTIA_DB` روی دیسک مانا ست شده
- [ ] `SECRET_KEY` ست شده (و در بکاپ تنظیمات ثبت شده)
- [ ] رمز مدیر پیش‌فرض عوض شده
- [ ] SMTP تنظیم شده (یا موقتاً `NODE_ENV` برای تست)
- [ ] وب‌اپ iOS روی صفحه‌ی گوشی نصب شده
- [ ] APK اندروید ساخته و `server.url` درست تنظیم شده
- [ ] EXE ویندوز ساخته شده (محلی یا ریموت)
- [ ] بکاپ روزانه‌ی دیتابیس فعال است

موفق باشید! 🛒
